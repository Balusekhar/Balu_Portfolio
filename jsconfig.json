{
    "compilerOptions": {
      "baseUrl": ".", // Root directory of your project
      "paths": {
        "@/*": ["src/*"] // Maps "@" to the "src" directory
      }
    },
    "exclude": ["node_modules"] // Exclude node_modules from IDE checks
  }